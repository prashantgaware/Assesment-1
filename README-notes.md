# Commission Calculator - Docs

## How to Run

### Backend (.NET API)
cd api
dotnet run

The API runs at `https://localhost:5000`. 
Swagger UI available at `https://localhost:5000/swagger`.

### Frontend (React)
cd ui
npm install
npm start

The app runs at `http://localhost:3000`.